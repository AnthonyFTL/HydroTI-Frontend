export default class IrrigationEntry {
    constructor(
        startDate,
        endDate,
        irrigationDuration
    ) {
        this.startDate = startDate;
        this.endDate = endDate;
        this.irrigationDuration = irrigationDuration;
    }
}
